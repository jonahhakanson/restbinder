# Dandelion Resource Example

The Dandelion Resource demonstrates lifecycle identity.

```txt
dandelion:yard-bed-04:0001
seed → germinating → sprout → vegetative → flowering → seeding
```

Each stage is a mutation of the same Resource, not a replacement object.
